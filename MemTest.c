// External definitions
extern unsigned char peek(unsigned int segment, unsigned int offset);
extern void poke(unsigned int segment, unsigned int offset, unsigned char data);

int main(void){
    // Insert Bus and Memory Test Code
    return 0;
}